spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: ilya
    password: '123'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8081